#!/usr/bin/env sh

# Get the directory where this script is located
SCRIPT_DIR="$( cd "$( dirname "$0" )" && pwd )"

# Execute the PHP script with all provided arguments
php "$SCRIPT_DIR/pr-code-check.php" "$@" 